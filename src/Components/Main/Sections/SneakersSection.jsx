export default function SneakersSection({ classes, classContainer }) {
  return (
    <section className={classes.section}>
      <div className={classContainer}>
        <div className={classes.sectionBlock}>2</div>
      </div>
    </section>
  );
}

export default function ProductSection({ classes, classContainer }) {
  return (
    <section className={classes.section}>
      <div className={classContainer}>
        <div className={classes.sectionBlock}>3</div>
      </div>
    </section>
  );
}

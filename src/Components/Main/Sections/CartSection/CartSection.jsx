export default function CartSection({ classes, classContainer }) {
  return (
    <section className={classes.section}>
      <div className={classContainer}>
        <div className={classes.sectionBlock}>4</div>
      </div>
    </section>
  );
}
